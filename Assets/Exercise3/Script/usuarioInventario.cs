using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class usuarioInventario : MonoBehaviour
{
    /* public Inventary inventario;
     [Tooltip("Elegi: Linterna, Cura, Arma.")] // esto permite meter texto en el inspector. Costo hallarlo jaja
     public string nombreObjeto;

     [Tooltip("Elegi la cantidad por favor un int")] // esto permite meter texto en el inspector. Costo hallarlo jaja
     public int cantidadObjeto;

     [Tooltip("Elegi si eliminar o agragar")]
     public string PonerOeliminar;
     void Start()
     {
         if (PonerOeliminar == "Agregar")
         {
             inventario.agregarObjeto(nombreObjeto, cantidadObjeto);
             Debug.Log("Nombre del objeto: " + nombreObjeto);
         }
         if (PonerOeliminar == "eliminar")
         {
             quitarObjeto(nombreObjeto, cantidadObjeto);

         }

     }

     // Update is called once per frame
     void Update()
     {

     }  */
}
